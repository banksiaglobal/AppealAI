export interface IResponseAddPackage {
  companyId: string;
  id: string;
  name: string;
  description: string;
}

export interface ICreatePackage {
  companyId: string;
  name: string;
  description: string;
}
